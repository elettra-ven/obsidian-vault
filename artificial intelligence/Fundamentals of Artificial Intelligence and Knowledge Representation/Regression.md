>[!info] **Regression** is the task of approximating a mapping function ($f$) from input variables ($X$) to a **continuous output** variable ($y$).

It's a **supervised** learning task, often used in statistic tasks.

It can be **multivariate** if the input data is described by multiple variables.
