>[!info] **Clustering** is the problem of organizing objects into **groups** whose members are similar in some way.

Clustering is used when the dataset is **unlabelled**, and is one of the main **unsupervised learning tasks**.

+ **Hard clustering**: 
	each element can be assigned to one and only one group 
+ **Soft or fuzzy clustering**:
	each element can belong to more than one group